name: synnex
description: "College App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # 🔥 FIREBASE
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.4
  cloud_firestore: ^5.4.4
  firebase_storage: ^12.0.3

  # 🎨 UI
  google_fonts: ^6.3.2
  intl: ^0.20.2

  # 🌐 NETWORKING & UTILS
  http: ^1.2.0
  url_launcher: ^6.2.5
  
  # 📹 VIDEO CALL (WEBRTC ADDED, JITSI REMOVED)
  flutter_webrtc: ^0.12.3

  # 📷 MEDIA & PERMISSIONS
  image_picker: ^1.1.1
  permission_handler: ^11.3.1
  
  # 🎵 AUDIO (Ringtone ke liye)
  audioplayers: ^6.0.0

  # 😄 EMOJI
  emoji_picker_flutter: ^1.6.3

  # 💾 LOCAL STORAGE
  shared_preferences: ^2.2.2

  # 🔥 SOCKET (REAL-TIME CHAT)
  socket_io_client: ^2.0.3+1
  firebase_messaging: ^15.2.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # ✅ ASSETS
  assets:
    - assets/sounds/
    - assets/images/
    - assets/icons/
    - shorebird.yaml